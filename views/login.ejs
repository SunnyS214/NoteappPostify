<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login</title>
  <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>
</head>
<body class="bg-zinc-700 text-zinc-300 min-h-screen flex flex-col justify-between">

  <main class="flex-grow container mx-auto px-4 py-8">
    <div class="flex justify-center items-center min-h-[80vh]">
      <form action="/login" method="post"
        class="w-full max-w-md bg-zinc-800 p-6 sm:p-8 rounded-lg shadow-lg flex flex-col gap-4 animate__animated animate__fadeInUp">
        
        <h1 class="text-2xl sm:text-3xl font-bold text-yellow-500 text-center">Login</h1>

        <input 
          type="email" 
          placeholder="Email" 
          name="Email" 
          class="outline-none p-3 rounded-lg bg-zinc-700 text-zinc-300 focus:ring-2 focus:ring-yellow-500 transition duration-300 w-full"
        />
        <input 
          type="password" 
          placeholder="Password" 
          name="Password" 
          class="outline-none p-3 rounded-lg bg-zinc-700 text-zinc-300 focus:ring-2 focus:ring-yellow-500 transition duration-300 w-full"
        />

        <button 
          type="submit" 
          class="w-full bg-yellow-500 text-black font-semibold p-3 rounded-lg hover:bg-yellow-600 transition duration-300"
        >
          Login
        </button>

        <div class="text-center mt-2 text-sm text-zinc-400">
          <a href="/signup" class="underline hover:text-yellow-500 transition duration-300">Create Account</a>
        </div>
      </form>
    </div>
  </main>


  

</body>
</html>
